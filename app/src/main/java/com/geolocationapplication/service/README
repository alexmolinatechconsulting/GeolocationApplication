There is no real business logic in the Geolocations service as it is simple crud.
As such, the GeolocationService class which would implement such logic using the GeolocationApi
just repeats what the Api does.